/** Stub CLI entry — team resource hub removed with OpenDesign Cloud. */
export async function runVelaCommand(_args: string[], _workspaceId?: string): Promise<string> {
  throw new Error('OpenDesign Cloud resource commands are not available in this build');
}
